[project]
name = "autocommit"
version = "0.1.0"
description = "Multi-repo auto commit & push with optional AI commit messages"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["rich", "argcomplete"]

[project.scripts]
autocommit = "autocommit.cli:main"
